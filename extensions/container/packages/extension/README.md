# Container Extension

A Kortex extension for managing containers.

## API

The extension exposes an API that can be used by other extensions. See the `api` folder for available methods and types. (extensions/container/packages/api/src/container-extension-api.d.ts)

## Requirements

- Container runtime (Docker, Podman, or compatible)
