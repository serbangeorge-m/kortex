{
  "name": "scheduler-native",
  "displayName": "Scheduler for macOS and Windows",
  "description": "A scheduler implementation using macOS LaunchAgents and Windows Task Scheduler",
  "version": "0.0.1-next",
  "icon": "icon.png",
  "publisher": "kortex",
  "license": "Apache-2.0",
  "engines": {
    "kortex": "^0.0.1"
  },
  "main": "./dist/extension.js",
  "scripts": {
    "build": "vite build && node scripts/build.js",
    "test": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest watch --coverage --passWithNoTests",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "@kortex-app/api": "workspace:*",
    "mustache": "^4.2.0",
    "inversify": "^7.10.3",
    "unzipper": "^0.11.6"
  },
  "devDependencies": {
    "@types/unzipper": "^0.10.11",
    "@types/mustache": "^4.2.6",
    "mkdirp": "^3.0.1",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
